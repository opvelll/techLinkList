type LinkData
  @model
  @auth(
    rules: [
      {
        allow: public
        provider: iam
        operations: [create, update, delete, read]
      }
    ]
  )
  @searchable {
  id: ID!
  title: String
  discription: String
  url: AWSURL!
  tag: [String]
}
